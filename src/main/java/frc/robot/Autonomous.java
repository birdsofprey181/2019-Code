//Gwen Miller 02/17/19

package frc.robot;

public class Autonomous {
    robot.doStuff()
    if(notWinning == true){
        robot.selfDestruct();
    }else if(notWinning == false){
        robot.victoryRoyale();
        robot.defaultDance();
    }
    /*
    if going for ship
        raise to ship
        drive to ship
        push hatch off
    else if going for rocket
        if level 1
            raise to level 1
        if level 2
            raise to level 2
        if level 3
            raise to level 3
        drive to rocket
        push hatch off
    else if not going for anything
        drive forward
    */
}